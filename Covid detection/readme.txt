DATASET 1: contains the CT scan images which are split into training and validation during runtime
DATASET 2: contains the CT scan images which are to be uploaded to the website for prediction testing
demo: the actual project folder to be run in the Pycharm IDE or any other IDE
Covid_detection file:used for performing preprocessing the dataset, data augmentation, training and testing of CNN model
Saved CNN: has the CNN AI model files used to make predictions on the images
wkhtml: the application which is used to generate PDF reports of the predictions
